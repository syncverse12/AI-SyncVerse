# ─────────────────────────────────────────────────────────────────────────────
# Real-Time Speech-to-Text — pinned production dependencies
# Base image: python:3.11-slim  (see Dockerfile)
# ─────────────────────────────────────────────────────────────────────────────

# Web framework + ASGI server
fastapi==0.117.1
uvicorn[standard]==0.36.0
starlette==0.48.0
python-multipart==0.0.20

# AssemblyAI SDK (same version as original Sentry AI repo)
assemblyai==0.35.0

# WebSocket support (required by uvicorn[standard] and assemblyai)
websockets==16.0

# HTTP client (assemblyai dependency)
httpx==0.28.1
httpcore==1.0.9

# Data validation
pydantic==2.11.7
pydantic-core==2.33.2
typing_extensions==4.13.2
annotated-types==0.7.0

# Async runtime
anyio==4.9.0
sniffio==1.3.1
h11==0.16.0

# Environment / config
python-dotenv==1.1.1
