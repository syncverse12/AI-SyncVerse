version: "3.9"

services:
  risk-microservice:
    build: .
    ports:
      - "7860:7860"
    env_file:
      - .env
    volumes:
      - ./data:/app/data   # persists the SQLite db across container restarts
    environment:
      - SQLITE_PATH=/app/data/risk_microservice.db
